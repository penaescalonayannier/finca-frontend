export interface Cliente {
  id?: string;
  cuenta: string;
  nombre: string;
  ruc: string;
  direccion: string;
}
export interface ClienteRequest {
  id?: string;
  cuenta: string;
  nombre: string;
  ruc: string;
  direccion: string;
}
